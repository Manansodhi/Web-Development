console.log("External Script")
alert("External Script hacked")
// if we write alert in external script with src then first "world" will be printed then "alert of head hacked" will appear then "external script will print" then "alert of external script hacked" will appear and  after this body content "Hello" get printed 